import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yuzeetest.mtm',
  appName: 'yuzeeMtmTestSearch',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
